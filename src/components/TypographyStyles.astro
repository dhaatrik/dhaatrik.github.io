---
// Typography plugin styles — loaded only on pages that render .prose content.
// Keeping this out of global.css avoids shipping ~80 KB of prose utilities on every route.
import '../styles/typography.css';
---